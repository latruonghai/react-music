import React from 'react';
import { ItemNavigatorProps, NavigationProps } from '../../../typings/props/components';

export const Navigation = (props: NavigationProps<ItemNavigatorProps>) => {
	return (
		<div></div>
	);
};

export default Navigation;